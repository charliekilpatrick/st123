# Pinned dependencies for jwst123 (Python 3.11–3.12).
# Referenced by pyproject.toml via [tool.setuptools.dynamic] dependencies.
# Validated with: conda create -n jwst123 python=3.12 pip && pip install -e .
# Install from the repo root:
#   conda create -n jwst123 python=3.12 pip
#   conda activate jwst123
#   conda install -c conda-forge hdf5 blosc pytables -y
#   pip install -e ./extdeps/jhat -e .
#
# JHAT is NOT pinned here: use the custom build under extdeps/jhat
# (see extdeps/jhat/README.md). Do not pip install jhat from PyPI for this repo.
#
# drizzlepac requires numpy>=2.

numpy==2.2.6
scipy==1.18.0
astropy==8.0.1
astroquery==0.4.11
astroscrappy==1.3.0
matplotlib==3.11.1
pandas==3.0.3
shapely==2.1.2
scikit-image==0.26.0
tqdm==4.69.0
requests==2.34.2
python-dateutil==2.9.0.post0
progressbar33==2.4
ccdproc==2.5.1
reproject==0.21.0
photutils==3.0.0
gwcs==0.26.1
asdf==5.3.1
jwst==1.20.2
drizzlepac==3.11.0
stwcs==1.7.7
stpsf==2.2.0
tables==3.11.1
