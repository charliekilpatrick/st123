# Pinned dependencies for st123 (Python 3.11–3.12).
# Referenced by pyproject.toml via [tool.setuptools.dynamic] dependencies.
# Validated with: conda create -n st123 python=3.12 pip && pip install -e .
# Install from the repo root:
#   conda create -n st123 python=3.12 pip
#   conda activate st123
#   conda install -c conda-forge hdf5 blosc pytables -y
#   pip install -e .
#   # or: pip install -e ".[dev]"
#
# Custom JHAT is installed from extdeps/jhat via the PEP 508 path line below
# (see extdeps/jhat/README.md). Do not pip install jhat from PyPI for this repo.
# tweakreg-hack is required at import time by extdeps/jhat.
#
# drizzlepac requires numpy>=2.

numpy==2.2.6
scipy==1.18.0
astropy==8.0.1
astroquery==0.4.11
astroscrappy==1.3.0
matplotlib==3.11.1
pandas==3.0.3
shapely==2.1.2
scikit-image==0.26.0
tqdm==4.69.0
requests==2.34.2
python-dateutil==2.9.0.post0
progressbar33==2.4
ccdproc==2.5.1
reproject==0.21.0
photutils==3.0.0
gwcs==0.26.1
asdf==5.3.1
jwst==1.20.2
drizzlepac==3.11.0
stwcs==1.7.7
stpsf==2.2.0
tables==3.11.1
tweakreg-hack==0.1.1
# Local custom JHAT (not PyPI). setuptools dynamic deps require PEP 508 form
# (no pip -e flag). Installed automatically with: pip install -e .
jhat @ file:./extdeps/jhat
